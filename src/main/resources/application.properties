quarkus.application.name=react-cms-courses-service
quarkus.http.port=${HTTP_PORT:8083}

# Datasource
quarkus.datasource.db-kind=mysql
quarkus.datasource.username=${DB_USER:}
quarkus.datasource.password=${DB_PASSWORD:}
quarkus.datasource.jdbc.url=jdbc:mysql://${DB_HOST:}:${DB_PORT:}/${DB_NAME:}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC

# Hibernate (schema owned by SQL scripts)
quarkus.hibernate-orm.schema-management.strategy=none
quarkus.hibernate-orm.log.sql=false

# Jackson camelCase (default) — explicit for clarity
quarkus.jackson.property-naming-strategy=LOWER_CAMEL_CASE
quarkus.jackson.write-dates-as-timestamps=false

# CORS
quarkus.http.cors.enabled=true
quarkus.http.cors.origins=http://localhost:5173,http://localhost:3000
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
quarkus.http.cors.methods=GET,POST,PUT,PATCH,DELETE,OPTIONS

# JWT (HS256 — verify with JWK; plain verify.secretkey is not a raw HMAC string)
mp.jwt.verify.issuer=${JWT_ISSUER:}
smallrye.jwt.verify.algorithm=HS256
smallrye.jwt.verify.key.location=jwt-secret.jwk

# OpenAPI / Swagger UI
quarkus.smallrye-openapi.path=/q/openapi
quarkus.smallrye-openapi.info-title=React CMS Courses Service
quarkus.smallrye-openapi.info-version=1.0.0
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.path=/q/swagger-ui

# Auth: public GETs; mutating requires authentication. JWT optional on public reads.
quarkus.http.auth.proactive=false

quarkus.http.auth.permission.public-docs.paths=/q/*
quarkus.http.auth.permission.public-docs.policy=permit

quarkus.http.auth.permission.public-get.paths=/api/courses/health,/api/courses,/api/courses/*,/api/lessons,/api/lessons/*
quarkus.http.auth.permission.public-get.methods=GET
quarkus.http.auth.permission.public-get.policy=permit

quarkus.http.auth.permission.authenticated.paths=/api/*
quarkus.http.auth.permission.authenticated.policy=authenticated
